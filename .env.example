# Optional — enables richer AI-powered natural language tag extraction
# via /app/api/nlp/route.ts. If left unset, the app automatically falls
# back to the local, offline keyword-based NL parser
# (lib/nlp/parseQuery.ts), so the app works perfectly with zero keys.
OPENAI_API_KEY=
